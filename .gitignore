# Rust
/target/
**/*.rs.bk

# Node / Vite
node_modules/
ui/dist/
ui/.vite/
*.tsbuildinfo

# Tauri generated
src-tauri/gen/
src-tauri/target/

# Built packs
packs/**/*.minostyle

# Editors / OS
.vscode/
.idea/
Thumbs.db
*.log

# Playwright MCP scratch output
.playwright-mcp/

# Build artifacts collected for hand-off
dist/

# The root package.json exists only to mark the project root and hold two
# scripts; it has no dependencies, so the lockfile pnpm writes beside it records
# nothing. The one that matters, ui/pnpm-lock.yaml, is committed.
/pnpm-lock.yaml